import { ReactElement } from 'react';
import { Wrapper } from './SetAvatar.styles';

const SetAvatar = (): ReactElement => {
	return <Wrapper>W oczekiwaniu na działający backend</Wrapper>;
};

export default SetAvatar;
