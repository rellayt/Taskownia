import styled from 'styled-components';

export const Wrapper = styled.div`
	background: linear-gradient(0deg, #f3f3f3, transparent, transparent, transparent, #f3f3f3);
	overflow-y: auto;
	display: flex;
	flex-direction: column;
	padding: 16px;
`;
